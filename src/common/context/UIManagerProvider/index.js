import UIManagerProvider, {
  useUIManagerContext,
  UIManagerContext,
} from "./UIManagerProvider";
import * as uiManagerReducer from "./uiManagerReducer";

export { useUIManagerContext, UIManagerContext, uiManagerReducer };
export default UIManagerProvider;
